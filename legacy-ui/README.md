# Legacy UI

The previous `app.html` UI is preserved in Git history on `main` and on the initial `ui-rebuild` commit. The active rebuild branch intentionally uses a clean UI shell so the existing functional modules can be rewired without the old presentation layer.
